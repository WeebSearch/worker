mutation(
  $rawName: String!
  $anilistId: Int
  $malId: Int
  $name: String
  $thumbnailUrl: String
) {
  createAnime(
    data: {
      rawName: $rawName
      anilistId: $anilistId
      name: $name
      malId: $malId
      thumbnailUrl: $thumbnailUrl
    }
  ) {
    id
  }
}
