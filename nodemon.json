{
  "restartable": "rs",
  "colours": true,
  "ext": "ts,graphql,json,.env",
  "exec": "tsc --skipLibCheck && node ./dist/index.js",
  "exclude": ["node_modules"],
  "events": {
    "restart": "echo \"Restarted due to $FILENAME\""
  }
}
